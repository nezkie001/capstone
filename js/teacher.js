// Functions for Teacher Interface

function addTeacher(name, subject) {
    // Code to add a new teacher
    console.log(`Teacher ${name} added for subject ${subject}.`);
}

function removeTeacher(name) {
    // Code to remove an existing teacher
    console.log(`Teacher ${name} removed.`);
}

function listTeachers() {
    // Code to list all teachers
    console.log("Listing all teachers...");
}

function assignTeacherToClass(teacherName, className) {
    // Code to assign a teacher to a class
    console.log(`Teacher ${teacherName} assigned to class ${className}.`);
}